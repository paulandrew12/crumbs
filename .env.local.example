# Cookie Chain endpoints. Defaults in src/lib/chain.ts match these,
# so the app runs with no .env.local at all.
NEXT_PUBLIC_COOKIE_RPC_URL=https://rpc.cookiescan.io
NEXT_PUBLIC_COOKIE_WS_URL=wss://wss.cookiescan.io
NEXT_PUBLIC_COOKIE_EXPLORER=https://cookiescan.io
